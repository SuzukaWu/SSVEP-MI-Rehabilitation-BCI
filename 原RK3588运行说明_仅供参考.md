> 修复说明：补回 `MIResultReceiver` 和 `mi_score_to_percent`，解决启动时报 `NameError` 的问题。

# SSVEP + MI 自由模式动图版

## 本次修改

本版本以“双模式修正版”为基础，只保留**自由使用模式**：

- 启动窗口不再显示“模式”选项；
- 不再包含指定目标、正确/错误、准确率、滚动窗口、自适应时长和标注片段；
- 八个目标仍按原频率和相位同时闪烁；
- 原 FBCCA 结果仍直接决定识别动作和气动手套指令；
- MI 仍只用于反馈显示，不参与 SSVEP 分类、手套控制或时序。

反馈界面修改：

- 删除右侧竖直绿色倒计时进度条；
- 左侧不再显示一张静态动作图片，而是循环播放识别动作对应的逐帧动画；
- 动画直接复用 `/home/cat/ssvep_rk3588/动作图/` 中原有的动作帧，不依赖 GIF 解码；
- MI 的横向“运动想象意图程度”模型指数条保留；
- MI 本轮没有数据时显示“暂无数据”。

## 保留不变的核心逻辑

- 原八个频率与相位；
- 原 LSL 采集和 CSV 写盘；
- 原写盘同步 `time.sleep(1)`；
- 原末段数据截取、250 Hz 重采样和 `fbcca.fbcca_classify()`；
- 原气动手套类别到串口指令映射；
- 普通动作反馈 7 秒，第1动作（握拳）反馈 14 秒；
- ESC 退出；
- MI 模型和 `online_opt_plus.py` 算法不修改。

## 安装

把压缩包上传到 `/home/cat/`，执行：

```bash
cd /home/cat
unzip -o SSVEP_MI自由模式动图版.zip
cd /home/cat/SSVEP_MI自由模式动图版
bash install.sh
```

安装脚本会备份当前程序，备份名称类似：

```text
/home/cat/ssvep_rk3588/stim_ssvep.py.bak_before_free_animation_时间戳
```

## 运行

先启动 BHB-EEG Station，连接头环并确认 `BHB-EEG` LSL 数据流正常。再在 RK3588 图形桌面终端运行：

```bash
/home/cat/ssvep_rk3588/run_ssvep_mi.sh
```

从 SSH 发起运行时，需要 SSH 会话已经获得 RK3588 图形桌面的 `DISPLAY` 和 X 授权。

## 日志

自由使用会话日志：

```text
<save_path>/ssvep_free_session_log.csv
```

MI 后台日志：

```text
/home/cat/ssvep_rk3588/logs/mi_online.log
```

## 回退

停止程序后，把安装时生成的备份复制回去：

```bash
cp /home/cat/ssvep_rk3588/stim_ssvep.py.bak_before_free_animation_时间戳 \
   /home/cat/ssvep_rk3588/stim_ssvep.py
```
