# 比6动作合并说明

本包以用户提供的 `ssvep_rk3588.zip` 为主程序基础，保留其中最新版自由模式、MI反馈、动作动图反馈、Windows适配脚本及RK3588启动脚本。

本次合并：

- 将运行目录 `动作图` 完整替换为 `external_program2` 中已经整理好的新动作图片；
- 第7个目标由原“握拳”改为“比6”；主界面动作顺序现为：握拳、比1、比2、比3、比4、比5、比6、特殊3；
- 比6静态图使用 `动作图/6.jpg`；
- 比6动画使用 `动作图/61.jpg` 至 `动作图/631.jpg`，共31帧；
- 第6目标继续使用张手动画，并按 external_program2 的方式反向播放原第7组帧；
- 串口指令映射保持不变：第7目标仍发送 `0xA6`；
- FBCCA、MI UDP接收、反馈UI、横向MI模型指数、日志和手套控制流程均保留自最新版SSVEP程序。

注意：压缩包中的历史 `.bak` 文件仅供回退；实际运行文件是根目录的 `stim_ssvep.py`。
